#!/bin/sh

set -e

if [ $# -ne 1 ]; then
  echo "Invalid usage"
  echo "usage: $0 MOVIE_FUN_HEALTH_URL"
  exit 1
fi

set -x
export MOVIE_FUN_HEALTH_URL=$1

BIN_DIR=$(cd $(dirname "$0") && pwd)
$BIN_DIR/shpec
